name: Update Kali AI Model

on:
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Update model instructions
        run: echo "Run update-model.sh on your Pi"
